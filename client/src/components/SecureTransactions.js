import React from "react";
import "../styles/FeaturePage.css"; // Shared CSS file for feature pages

const SecureTransactions = () => {
    return (
        <div className="feature-page">
            <h1 className="feature-title">Secure Transactions</h1>
            <p className="feature-description">
                Tapay ensures your transactions are safe and secure with end-to-end encryption
                and advanced fraud detection systems.
            </p>
        </div>
    );
};

export default SecureTransactions;
